button.btn(
  type="button"
  data-toggle="modal"
  data-target="#editSelfModal"
)
  i.fas.fa-edit
div.modal.fade#editSelfModal(
  tabindex="-1"
  role="dialog"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
)
  div.modal-dialog(role="document")
    div.modal-content
      div.modal-header
        h1 Change Password
        button.close(
          type="button"
          data-dismiss="modal"
          aria-label="Close"
        )
          span(aria-hidden="true") &times;
      div.modal-body
        p form here
      div.modal-footer
        button.btn(type="submit") Submit
